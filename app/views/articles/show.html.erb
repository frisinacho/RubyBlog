<table>
  <tr>
    <th>Title:</th>
    <th>Text:</th>
    <th colspan="2"></th>
  </tr>
      <tr>
        <td><%= @article.title %></td>
        <td><%= @article.text %></td>
      </tr>
</table>

<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>
